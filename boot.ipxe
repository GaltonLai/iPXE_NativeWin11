#!ipxe
dhcp

set base_url https://raw.githubusercontent.com/GaltonLai/iPXE_NativeWin11/main

kernel ${base_url}/wimboot
initrd ${base_url}/bootmgr bootmgr
initrd ${base_url}/BCD BCD
initrd ${base_url}/boot.sdi boot.sdi
initrd -n boot.wim ${base_url}/boot.wim

boot
